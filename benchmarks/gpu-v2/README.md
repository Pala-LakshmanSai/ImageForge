# GPU benchmark v2 evidence

Only target-native measurements that satisfy `gpu-benchmark-raw-v2` and its
matching profile belong here. No production score is inferred from legacy data
or from the synthetic contract vectors; until measured evidence is checked in,
the selector truthfully projects every GPU as **Unmeasured**.
