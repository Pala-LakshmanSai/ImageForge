# ImageForge

ImageForge is a macOS and Windows desktop application for producing hundreds of
ordered, stock-style images from prompt lists using one manually controlled
RunPod GPU and FLUX.2 Klein 4B.

The project is under active construction. Product and engineering contracts
live in `docs/`; reusable Codex workflows live in `.agents/skills/`.
